---
name: exam-maker
description: 通用命题/组卷智能体。面向任意课程，依据往年真题自动完成"真题解析→考点分类→双向细目表→试卷模板提取→按难度配比生成 N 套带详细参考答案与评分标准的新卷→转换为 latex/docx/md 并交教师选卷"。每个重活步骤启动子代理"分析 + 核对"，含教师人工确认与选卷环节（教师主责）。当用户要求"出试卷/命题/出N套卷/组卷/模拟卷/期末卷/半期卷/根据往年真题出题/据此出一套题"时使用。
---

# 通用命题（出卷）流程

面向**任意课程**、依据**往年真题**命制新卷的通用智能体。核心承诺：

- **结构对齐真题**：题型、题量、单题分值、总分、时长与真题一致（除非用户另有要求）。
- **考点驱动组卷**：先建**双向细目表**（考点 × 认知层次/难度 × 题型 × 分值），再据表命题，多套合计覆盖大纲考点。
- **难度可核算**：默认按**分值**配比 `基础60% / 中等30% / 难10%`（可改），生成后逐套核算达标。
- **不抄原题**：可计算学科同型变形（换数据、答案整齐）；概念学科换情境/换设问角度。套与套之间、对真题均不重复。
- **每题可核验**：可计算学科用脚本**逐题验算**到全 PASS；概念/文科学科给"参考答案 + 评分要点 + 依据/出处"并由复核子代理核对（必要时联网核实事实）。
- **专业交付**：含**详细参考答案**与**分步评分标准**；输出 `latex`（可编译）并可转 `docx / md`。
- **教师主责**：解析、考点分类、模板、成卷四处设**人工确认/调整点**，最终由教师**选卷**。

> 这是一个**编排型**技能：主智能体串联流程，在步骤 2–5 各**启动子代理**完成"分析"并再做一遍"核对"，把结果交回主线；不确定处停下来问教师。详细做法见 `references/` 三份手册，可计算学科的题型变形范例见 `examples/linear-algebra/`。

## 参数（开工前先与用户对齐，能推断的就推断，缺失的只问关键项）

| 参数 | 默认 | 说明 |
|---|---|---|
| `COURSE` | 从真题推断 | 课程名。命题人可给，也可留空由子代理从真题归纳。 |
| `SCOPE` | 空（由真题归纳） | 命题范围/章节/考点分类。可给"考纲、知识点清单、题型分类"，也可不给。 |
| `PAST_PAPERS` | 与输出同目录 | 往年真题文件：`pdf / docx / doc / tex / md`，一份或多份。 |
| `OUTPUT_FILE_TYPE` | `latex` | 成卷格式：`latex`（默认）/ `docx` / `md`。 |
| `DIFFICULTY` | `基础60% 中等30% 难10%` | **按分值**的难度配比，可改（如半期卷、竞赛卷）。 |
| `N_SETS` | `8` | 生成套数。 |
| `WORKDIR` | `PAST_PAPERS` 所在目录 | 成卷默认写回此目录；**中间产物**写入其下 `exam-build/`。 |
| `VERIFY_MODE` | `auto` | 学科类型：`auto` 自动判定 / `computational` / `conceptual` / `mixed`。见 `references/quality-gates.md`。 |

难度定义（用于核算，可计算与概念学科通用，细则见 `references/blueprint-and-difficulty.md`）：
**基础/送分**＝单一考点、直接套用、步骤少；**中等**＝2 个考点综合或需一步转化；**难**＝核心方法综合/严谨论证/建模，但**不偏不怪、不超纲**。配比按**分值**核算（不是按题数），允许把一道大题拆成不同难度的小问来凑比例。

## 工作流程

> 命名约定：中间产物统一放 `WORKDIR/exam-build/`，成卷放 `WORKDIR/`（可让用户改）。每个"启动子代理"步骤，先派一个**分析子代理**产出结果，再派一个**核对子代理**（或让分析子代理自查一轮）比对来源/查错，主线只接收通过核对的产物。子代理提示词模板见 `references/quality-gates.md`。

**步骤 0：探测环境与对齐参数。** 确认 `PAST_PAPERS` 位置与格式；按 `references/io-and-conversion.md` 的"环境探测"清单检测 `pandoc`、`python + sympy/numpy`、LaTeX 引擎（`xelatex/latexmk/tectonic`）、`.doc` 转换器是否可用，**缺什么当场告诉用户并给出降级方案**（例如无 LaTeX 引擎则只交付可在 Overleaf 编译的 `.tex` 源、跳过本地编译）。判定 `VERIFY_MODE`。建 `exam-build/`。

**步骤 1：真题 → LaTeX（启动子代理，逐份解析 + 核对）。** 依格式处理（详见 `references/io-and-conversion.md`）：
- **PDF**：用 **Read 工具逐页视觉识读**（即 OCR），忠实转成 LaTeX（正文、公式、表格、图注、分值标注都要），长文档按页码区间分片并行；再派**校对子代理**把转出的 LaTeX 与原页逐页比对，改到无遗漏/无错读。
- **docx**：`pandoc in.docx -o in.tex`（公式、表格一并转）；结构复杂时辅以 `python-docx`。
- **doc（旧版）**：先转 `docx`（LibreOffice/Word，若无则请用户另存为 docx）再走上条。
- **tex / md**：规范化为可用本技能导言区编译的 LaTeX（md 用 `pandoc in.md -o in.tex`）。
产出：`exam-build/source-<name>.tex`（每份真题一个，忠实还原）。

**步骤 2：考点分类 → 双向细目表（启动子代理，分析 + 核对；⏸人工确认）。** 派**考点分析子代理**：逐题判定主考考点、题型、分值、难度、认知层次，按考点归类，产出**双向细目表** `exam-build/blueprint.md`（含考点清单、题目→考点映射 `blueprint.jsonl`、难度/分值分布）。若用户给了 `SCOPE` 就对齐其分类，否则自下而上归纳。再派**核对子代理**检查分类一致性与覆盖度。**⏸ 停下来把细目表交教师确认/调整**（可合并拆分考点、改归类、标重点/必考）。做法与表结构见 `references/blueprint-and-difficulty.md`。

**步骤 3：试卷模板提取（启动子代理，分析 + 核对；可与步骤2合并汇报）。** 派**模板子代理**从真题抽取试卷模板：题型序列、各题型题量、单题与小问分值、总分、时长、抬头/须知/评分说明风格、排版体例，产出 `exam-build/template.md`（结构表 + 分值核对，Σ=总分）。核对分值合计与真题一致。**⏸ 与细目表一起交教师确认**（题型是否调整、总分/时长是否变）。

**步骤 4：难度配比落到模板（分析 + 核算）。** 依 `DIFFICULTY` 把难度指派到模板各题位/小问，使 Σ基础/Σ中等/Σ难 的**分值占比**命中目标（默认 60/30/10，容差 ±3%），产出"难度核算表"（并入 `template.md`）。可用 `references/blueprint-and-difficulty.md` 的核算清单或一段小脚本自检。

**步骤 5：生成 N 套新卷（启动子代理，生成 + 逐题核验；产出 latex）。** 依"细目表 + 模板 + 难度核算 + 防重台账"命题（策略见 `references/blueprint-and-difficulty.md`）：
- 派**命题子代理**（可每套一个或分批），按题位设计题目——**先定答案结构，再反向构造题面**（计算题保证消元/结果整齐；概念题换情境）。多套在同一题位轮换不同变形手法/情境，合计覆盖细目表考点。
- 派**核验子代理**按 `references/quality-gates.md` 把关：可计算学科**复制 `scripts/verify_template.py`** 为本次脚本，给**每道题**写 `chk()` 断言跑到全 PASS（FAIL 就改题面重验，禁止改答案迁就）；概念学科逐题核对"答案是否真正回答设问、评分要点是否完备、事实/出处是否可靠"（必要时联网）。
- 用 `assets/preamble.tex` 导言区写 LaTeX：文件含【命题说明与考点/难度分析】→ 每套【试题页】→【参考答案与评分标准页】，每个得分步骤后紧跟 `\score{n}`，题分合计准确、每套总分达标。
- 把每套用到的"考点 + 变形手法 + 关键数据/情境"追加进**防重台账** `exam-build/ledger.md`，确保不与真题、已生成各套重复。
产出：`exam-build/paper-1.tex … paper-N.tex`（或按用户要求合并为一个 `.tex`）。

**步骤 6：编译/转换 + 交付选卷（⏸人工选择）。**
- 若检测到 LaTeX 引擎：编译每套两个来回（`latexmk -xelatex` 或 `xelatex -interaction=nonstopmode -halt-on-error`），修掉编译错误与 `Overfull`（长公式链拆成两个 display 块）；无引擎则跳过并说明。
- 按 `OUTPUT_FILE_TYPE` 转换（`references/io-and-conversion.md`）：`docx`/`md` 用 `pandoc` 从 `.tex` 转出，**转完派子代理抽查**公式、表格、`\score` 分值、评分说明是否完整，缺失就回改源或调整转换参数。
- **⏸ 向教师汇报并交付选卷**：给出每套的考点覆盖、难度构成（分值占比）、与真题的变形对应、验算通过数（如 92/92）、编译/转换结果；请教师**挑选**要采用的若干套，把选中的成卷放到 `WORKDIR/`。

## 质量红线（交付前自检）

- [ ] **结构**：题型/题量/分值/总分与模板一致，每题 `\score{}` 之和＝题分，每套总分达标；
- [ ] **难度**：Σ基础/中等/难 分值占比命中 `DIFFICULTY`（默认 60/30/10，容差 ±3%）；
- [ ] **可核验**：可计算学科每题有验算 PASS 记录、送分题真送分（可口算/常规算法整数落地）；概念学科每题答案对应设问、评分要点完备、事实有据；
- [ ] **多样、不同形**：同一考点在 N 套间**换了变形轴**（正逆/任务/表征/含参/综合/情境/粒度），不是只换数字照搬设问；与真题同考点不同形态（台账按"考点+轴+设问范式"判重，见 `references/blueprint-and-difficulty.md`）；
- [ ] **覆盖**：N 套合计覆盖细目表主要考点；难题属大纲内综合、不超纲不偏怪；
- [ ] **可交付**：LaTeX 编译零错误零 Overfull（若有引擎）；转 docx/md 后内容无丢失；
- [ ] **教师主责**：细目表/模板/成卷三处已获教师确认，最终由教师选卷。

## 注意

- **能推断就别追问**：课程名、题型结构、总分多能从真题读出；只在"范围取舍、难度配比、总分/时长变更、选卷"这类真正属于教师的决定上停下来问。
- **半期卷/改范围**：`SCOPE` 收窄时，把超范围的题位换成范围内考点，题型分值结构不变（除非用户要求改）。
- **N 很大时**：同题位优先轮换不同**变形轴/设问范式**（正↔逆、含参、综合、换表征…），而非只换数字；变形轴用尽后再复用结构手法时，数据/情境须全新。台账按"考点+轴+范式"判重。
- **中间产物**（解析 tex、细目表、验算脚本、日志）留在 `exam-build/`；验算脚本可放 `%TEMP%` 不入云盘目录。
- 详细手册：`references/blueprint-and-difficulty.md`（细目表+难度+变形+防重）、`references/io-and-conversion.md`（各格式进出转换+环境探测）、`references/quality-gates.md`（学科自适应核验+子代理提示词+红线）；可计算学科范例见 `examples/linear-algebra/`。
