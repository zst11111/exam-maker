name: CI
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python -m compileall -q web
      - run: node --check web/static/js/charts.js
      - run: node --check web/static/js/teacher.js
